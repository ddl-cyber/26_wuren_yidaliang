# 学习历程
由于这是一个填空题的项目，我只需要：
1.K-Means算法流程
2.理解每个模块的实现
3.模块之间变量的传递实现即可。
因此，主要是与AI不断交互，理解逻辑，并没有看视频。

# 结果分析和猜想
1.可以看出经过聚类计算，center不断调整，最终返回的center就是聚类中心（先随机选四个点，距离最小的点归到center，然后再用质心更新center）。
2.cost曲线证明了聚类的收敛性，cost值在迭代过程中逐渐减少，最终收敛到一个最小值。

# AI
自己填充模块，AI检查错误、帮助理解。